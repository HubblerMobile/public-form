version: 0.1
phases:
    pre_build:
        commands:
            - 'npm install'
    build:
        commands:
            - 'npm run-script sandbox-build'
artifacts:
    files:
        - appspec.yml
        - 'forms/**/*'
    discard-paths: no